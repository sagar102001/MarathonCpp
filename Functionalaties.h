#include <vector>
#include <functional>
#include <thread>
#include <string>
#include <functional>
#include <future>

// function find the addition of two numbers
 

// function find the sum of N numbers in vector
void SumOfnumbers(std::vector<int> &res, int N);

// function find the odd number is the vector
void OddNumbers(std::vector<int> &vec);

// threads executions
void ThreadExecution(std::array<std::thread, 3> &thread);
